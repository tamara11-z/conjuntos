```javascript
function seleccionar(materia) {

    const resultado = document.getElementById("resultado");

    resultado.innerHTML =
        "✅ Has seleccionado <strong>" +
        materia +
        "</strong>. Esta respuesta se utilizará para formar los conjuntos.";
}
```
